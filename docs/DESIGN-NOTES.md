# CHERNOBYL: ECHOES OF ZERO — Part 1: THE FALL
### نسخة قابلة للعب (Vertical Slice) — ماذا نُفّذ من وثيقة التصميم، وماذا بقي

الملفات: `chernobyl/index.html` (اللعبة كاملة بملف واحد) + `chernobyl/three.min.js` (Three.js r160 — المحرّك ثلاثي الأبعاد، يُحمَّل محليًا بلا CDN). تعمل في المتصفح مباشرة، أوفلاين.

---

## 1) ما نُفّذ من الوثيقة (مطابقة صفحة بصفحة)

| صفحة الوثيقة | التنفيذ في النسخة القابلة للعب |
|---|---|
| 01 — Core Idea | لا وحوش عشوائية. التقدّم = معرفة. الهدف يتغيّر مع كل اكتشاف، والسؤال النهائي يظهر في الملف الأخير. |
| 02 — The World | مدينة كاملة + غابة + منشآت. أحداث غير طبيعية بلا أي رسالة «PSYCHOLOGICAL EVENT STARTED»: رقم لافتة يتغيّر، ثلج يظهر ويختفي، ممر يمتد، غرفة تُضاء بلا كهرباء. |
| 03 — Four Layers | متغيّر `G.echo` يتحكّم بالطبقة: REALITY → MEMORY → PSYCHOLOGICAL → ZERO. يظهر في الدفتر، ويغيّر الإضاءة والحبوب والتشويش وسلوك الكيانات. |
| 04/05 — Alexei | ليس جنديًا خارقًا: 3 إصابات، تحمّل محدود للجري، انحناء، موارد شحيحة. ماضي إيلينا موجود في الملفات (مريضة 1986، سجل الولادة). |
| 06 — Black Site-4 / ECHO / ZERO | أنفاق + BLACK SITE-4 (غرفة المراقبة، المختبر، الأرشيف) بملفات ECHO و ZERO. |
| 07 — The Twist | موجود حرفيًا في نهاية الجزء الأول: `STATUS: NOT CREATED / NOT MODIFIED / ORIGINAL` ثم `PROJECT ZERO WAS CREATED TO REPRODUCE HIM.` |
| 08 — Elena | 3 أشرطة **متناقضة** (الثالث يقول «لم أكن حاملًا أبدًا» ثم «هذا لم أكن أنا»). |
| 09/10 — Nika | Subject N-07، تعرف الأنفاق، تقول «You were here before you were born»، ولها نظام ثقة خفي (بلا أرقام ظاهرة) يغيّر الحوار والنهاية. |
| 11 — Rex | مؤشر نفسي/بيئي: ينبح على الفراغ، يحدق في زاوية، يتجمّد قبل الخطر، **يرفض دخول الأنفاق**. |
| 12 — Creatures | Failed Subjects (غير مستقرة، أبطأ إن كانت مصابة) و Retained Subjects (تتعلّم: تعود لتفتّش أماكن اختبائك السابقة) و Psychological Entities (تظهر في طرف النظر فقط وتختفي حين تنظر). |
| 13 — Subject 01 | يظهر في الجزء الأول كظلّ لحظي في الأرشيف، بلا قتال. |
| 14/30 — Horror philosophy | لا jump scares متكررة: ترقّب، صمت، صوت، ظل. `Anticipation` عبر مؤشر `?` فوق العدو ونظام نبض القلب. |
| 15/16 — Camera | كاميرا قريبة فوق الكتف، مع إزاحة في اتجاه النظر، ومجال رؤية محدود، واهتزاز. الإضاءة/الظل يُحسبان بشعاعات حقيقية — ما خلف الجدار لا يُرى. |
| 17 — Audio | مولّد صوتي بالكامل (WebAudio): رياح، همهمة داخلية، خطوات، جايجر، نبض، همس، صرير، صمت. |
| 18 — Radio | <kbd>R</kbd>: رسائل لا تعرف إن كانت مباشرة أو قديمة أو منك. أحيانًا نفس الصوت بجملتين مختلفتين. |
| 19/20 — Zones & the city as a character | 9 مناطق مترابطة. كل منطقة لها `visits` و`shiftFn`: تعود إليها فتجد بابًا لم يكن موجودًا، صورة خامسة، أكياس جثث زائدة، ثلجًا في الشارع. |
| 21–26 — Puzzle chains | سلسلة حقيقية كاملة (انظر §2). أنواع الألغاز: بيئية، ملاحظة، تسلسل، خريطة، صوت، ذاكرة، عودة (backtracking)، قصصية، نفسية. |
| 27/28 — Combat & resources | `Observe → Decide → Survive`. لا سلاح فعّال: هروب/اختباء/تغيير طريق. بطارية كشاف محدودة، بطاريات، حقيبة إسعاف، إشعاع متراكم. |
| 29 — Safe areas | كل منطقة لها نقطة آمنة (مكان الحفظ/الإحياء) — لكن الأنومالي قد يصل إليها. |
| 31/32 — Environmental & side stories | 17 مستندًا: يوميات جار، سجل إخلاء مدرسة (412/413)، ملاحظة معلّمة، سجل ورديات، صور عائلية… معظمها لا علاقة له بـ ZERO. |
| 33 — Weather | ثلج يظهر ويختفي بلا تفسير (أنومالي + جزيئات). |
| 34/35 — Returning & player memory | عدّاد زيارات لكل مشهد + تغييرات دائمة + خريطة تصبح غير موثوقة بعد الأنفاق. |
| 36/37 — Unreliable memory / big mystery | ذاكرة ألكسي تُعرض كـ«فلاش 1986» عند العجلة، وتسجيلات إيلينا تتناقض مع الملفات. |
| 38–52 — Part 1 beats | التحطّم ← الغابة والجنود ← بريبيات ← نِيكا ← ركس ← أول حدث ذاكرة ← أشرطة إيلينا ← المحطة والتيار ← المستشفى ← القبو ← الأنفاق ← BLACK SITE-4 ← الملف الأخير. |
| 56 — Endings | 3 نهايات في نهاية الجزء (A/Escape، D/ZERO، E/Original) + ملخّص قرارات نِيكا. |
| 57–61 — Progression / HUD / map / inventory | تقدّم = Knowledge + Access + Memory + Trust + World State. HUD شبه معدوم. خريطة تُرسم ذاتيًا وتكذب لاحقًا. دفتر (Tab) فيه المهمة/الملفات/الأشرطة/الرموز/الخريطة/الملاحظات. |
| 62 — Gameplay loop | Explore → Observe → Clue → Puzzle → Access → Danger → Avoid/Confront → Return → Change → Story → Deeper. |
| 65 — Design rules | القواعد العشر محترمة: لا superhuman، لا وحوش كل 5 دقائق، ليس كل شيء مرتبطًا بـ ZERO، ليس كل لغز مفتاحًا، لا شرح كامل، لا jump scares، نِيكا ليست ضعيفة، ركس ليس ديكورًا، المدينة ليست خلفية، اللاعب لا يتيقن. |

---

## 2) سلسلة الألغاز كما نُفّذت (Puzzle Chain)

```
التحطّم (غابة، جنود، إشعاع)
   ↓  اتجه جنوبًا
بريبيات  ← عدّاد جايجر، مخلوق جريح، عجلة الهواء (فلاش ذاكرة 1986)، ركس
   ↓
الشقة 16  ← صورة عائلية + يوميات جار (قصة جانبية)
            مشغّل أشرطة، بطاريات، مفتاح الحمّام
            تحت السرير: شريط إيلينا 1  →  «لا تثق بما تتذكر»
            رسمة طفل  →  الرمز 1 (الذرة)
            ورقة الحمّام  →  الترتيب: «غرفته ← المدرسة ← الروضة ← ثم تحت»
   ↓
المدرسة  ← صورة ثلاثة مبانٍ = كود لوحة المحطة (ذرة/موجة/عين)
            خريطة المنطقة → الرمز 4 (العين) + موقع المحطة
            لقاء نِيكا: «الكتلة C ليست مقفلة، هي ليست هنا بعد»
            → تعود إلى المدخل فتجد بابًا لم يكن موجودًا
            الكتلة C → الرمز 2 (الموجة) + لوحة BLACK SITE-4
   ↓
روضة الأطفال ← صندوق الموسيقى → الرمز 3 (الصفر) + ملاحظة المعلّمة
   ↓
محطة التحويل ← لوحة: الذرة ← الموجة ← العين  →  عودة التيار للمدينة كلها
                 الخزانة: بطاقة الوصول + شريط إيلينا 2 (الترتيب + N-07)
   ↓
المستشفى ← ملف مريضة 1986 (إيلينا)، سجل ولادة (الرقم 00 في 2026)
             باب القبو = بطاقة + تيار
   ↓
القبو ← أكياس جثث (الثالث دافئ)، ملف ECHO → درج/مصعد
   ↓
الأنفاق ← غرفة مراقبة (كاميرا تعمل بلا كهرباء منذ 40 سنة)
            مفتاح الأرشيف، مخلوق Retained يتعلّم أماكن اختبائك
   ↓
BLACK SITE-4 ← الأرشيف: ECHO الكامل، ZERO (Subject 01 / N-07 / 97%)
                شريط إيلينا 3 (يتناقض مع الأول)
                الظلّ الذي يعرف اسمك
                الملف الأخير → ORIGINAL → 3 نهايات
```

---

## 3) التحكم

| المفتاح | الوظيفة |
|---|---|
| `WASD` / الأسهم | الحركة |
| الفأرة | اتجاه النظر (الكاميرا فوق الكتف) |
| `Shift` | جري (ضجيج أعلى + تحمّل) |
| `C` / `Ctrl` | انحناء (أهدأ) |
| `E` / نقرة | تفاعل |
| `F` | الكشاف (يجذب الانتباه) |
| `H` | اختباء داخل الأثاث |
| `R` | الراديو |
| `Q` | حقيبة إسعاف |
| `Tab` / `J` / `M` | الدفتر / الخريطة |
| `Esc` | إيقاف • `F5` حفظ |

على الجوال: عصا لمس + أزرار تفاعل/كشاف/انحناء/دفتر.

---

## 4) ما لم يُنفَّذ بعد (خارطة الطريق للجزء الأول الكامل)

1. **أصول ثلاثية الأبعاد**: النسخة الحالية 2.5D مرسومة بالكود (بلا ملفات خارجية). للترقية إلى «over-the-shoulder» حقيقية: Unity/Unreal أو Three.js مع نفس منطق الأنظمة — كل الأنظمة هنا مكتوبة بحيث تُنقل كما هي (Layers, Anomalies, Flow-field AI, Puzzle graph, Trust).
2. **Subject 01** ككيان متعلّم فعليًا (حاليا ظهور لحظي + آثار).
3. **مناطق إضافية**: قصر الثقافة، المسبح، الفندق، المنطقة الصناعية — موجودة كمبانٍ مقفلة في المدينة؛ تحتاج داخليات.
4. **Part 2/3**: البنية جاهزة (`SCENES`, `flags`, `endings`) لكنها خارج نطاق النسخة التجريبية.
5. **دبلجة كاملة**: حاليًا WebAudio مولّد + `speechSynthesis` اختياري للهمسات والتسجيلات.
6. ~~أنيميشن للشخصيات~~ → **تم في محرّك 3D**: هياكل منخفضة المضلّعات (low-poly rigs) بأطراف متحرّكة لأليكسي والجنود والمخلوقات وركس ونيكا، مع انحناءة ظهر للمخلوقات واتجاه نظر لركس.

---

## 5) ملاحظات تقنية

- `index.html` (~228 KB) + `three.min.js` (654 KB)، يعمل أوفلاين بلا CDN.
- **محرّكان**: 3D (Three.js/WebGL — الافتراضي) و 2.5D (Canvas 2D + إضاءة بشعاعات) يبقى كخيار احتياطي/للأجهزة الضعيفة. التبديل بـ <kbd>B</kbd> أو من القائمة، ولا يُفقد أي تقدم.
- الذكاء الاصطناعي: BFS flow-field نحو آخر موقع معروف، مع رؤية (زاوية + مدى + خط نظر) وسمع (حسب الجري/الانحناء/الاختباء) وضوء الكشاف كعامل جذب.
- الحفظ: `localStorage` تلقائي كل ~25 ثانية + عند كل انتقال مشهد + يدوي `F5`. يشمل حالة الأبواب، العناصر الملتقطة، مواقع الأعداء (المدينة «تتذكر» أين كانوا)، الرموز، الثقة، والطبقة.
- تم اختبار اللعبة آليًا: اتصالية كل الخرائط (9 مشاهد، كل الأبواب والمستندات والأعداء قابلة للوصول) + مسار لعب كامل من التحطّم إلى النهاية + محاكاة 1000 إطار مع كل الأنومالي والحوارات دون أخطاء.


---

## 6) المحرّك ثلاثي الأبعاد (3D)

اللعبة الآن **3D حقيقية** (WebGL / Three.js r160)، مع الحفاظ على كل فلسفة الوثيقة.

**المبدأ المعماري:** شبكة البلاطات (tile grid) تبقى **مصدر الحقيقة الوحيد** — التصادم، الذكاء الاصطناعي، إيجاد المسار (BFS flow-field)، الأنومالي، الإضاءة المنطقية، والقصة كلها لم تتغيّر. الطبقة ثلاثية الأبعاد هي **طبقة عرض فقط**، لذلك لا يمكن لـ 3D أن تكسر أي لغز أو مسار قصصي.

| العنصر | التنفيذ |
|---|---|
| هندسة العالم | كل مشهد يُحوَّل إلى BufferGeometry مدمج: الأرضيات تُدمج في شرائح أفقية، والجدران تُرسم **أوجهها المكشوفة فقط** مع حساب فرق الارتفاع بين الجيران. النتيجة: مشهد المدينة كله = عقدتان فقط (~27,800 مثلث) بدل آلاف. |
| المواد | MeshLambertMaterial + vertex colors: كل بلاطة تحمل تباينًا لونيًا (hash) وAO مُعتِم قرب الجدران. نسيج إجرائي لكل مادة (خرسانة، جص، معدن، خشب، سيراميك، مختبر، تراب، أسفلت، صدأ…) — بلا أي ملفات صور. |
| الارتفاعات | جدران داخلية 3.2م، هياكل المباني 5.6م مع سقوف، الأسوار قضبان رفيعة، الماء شبكة شفافة متموّجة. |
| الأغراض | ~45 نوعًا: لوحات billboards بألفا (أشجار، سيارات، حافلات، أعمدة إنارة، نار، مقاعد، لافتات، حطام) + صناديق ثلاثية الأبعاد (أثاث، آلات، أسرّة، خزائن، غلاية، محوّلات، شاشات). **العجلة الدوّارة بنية 3D فعلية** (حلقة + 16 قضيبًا + كبائن تبقى معتدلة أثناء الدوران). |
| الشخصيات | هياكل low-poly بأطراف متحرّكة: أليكسي (تراه أمامك في كاميرا فوق الكتف — وهذا جوهر الوثيقة)، الجنود (مع بندقية)، المخلوقات (منحنية، عيونها تتوهّج عند المطاردة)، ركس (أرجل وذيل واتجاه نظر)، نيكا (بهالة خفيفة وشفافية حسب `vis`)، والكيانات النفسية = ظل طويل يظهر ويختفي. |
| الإضاءة | ضباب أسي كثيف (FogExp2)، ضوء نصف كروي بارد، **كشاف SpotLight ملتصق بالكاميرا يرمي ظلالًا حقيقية**، ومجمع 6 PointLights يُسند كل إطار لأقرب مصادر الضوء (أعمدة، نيران، مشاعل الجنود) مع الوميض (`flick`) وأضواء الذاكرة (`memory`). |
| الكاميرا | **فوق الكتف** (افتراضي، قريب، مع إزاحة يمينية، اصطدام بالجدران يسحبها للداخل، اهتزاز مشي، وميل عند الإصابة) + **منظور أول** بـ <kbd>V</kbd>. ارتفاع العين 1.64م واقفًا / 1.06م منحنياً / ينخفض عند الاختباء. |
| الجودة | عالية/متوسطة/منخفضة بـ <kbd>G</kbd> (ظلال 1024/512/بدون + pixelRatio)، **وتخفيض/رفع تلقائي** حسب قياس إطار/ثانية، مع رجوع تلقائي إلى 2.5D إن لم يتوفر WebGL. |
| ما بعد المعالجة | إطار WebGL يُنسخ إلى canvas ثنائي الأبعاد فتبقى كل تأثيرات التشويش/الحبوب/الطبقات (glitch, grain, memory tint, damage vignette) تعمل كما هي فوق الصورة ثلاثية الأبعاد. |

**مفاتيح جديدة:** <kbd>B</kbd> تبديل المحرّك 3D/2.5D · <kbd>V</kbd> تبديل الكاميرا · <kbd>G</kbd> الجودة — إضافة إلى أزرار أعلى يسار الشاشة وزر في القائمة الرئيسية وقائمة الإيقاف.

**الاختبارات (كلها ناجحة):**
- `3D smoke test`: 120 إطارًا + 9 مشاهد × 60 إطارًا + تحريك الكاميرا، مع التحقق من عدد العقد والمثلثات والأغراض والشخصيات والأضواء لكل مشهد.
- `geometry test`: حدود y/x/z داخل شبكة كل مشهد، الأرضيات عند y≈0، ارتفاعات الجدران/السقوف سليمة، والعالم مدمج في عقدتين.
- `full playthrough in 3D`: اتصالية 9 مشاهد + جمع العناصر + الرموز الأربعة + الطابق السفلي ← الأنفاق ← Black Site-4 + النهاية + حفظ/تحميل — كلها داخل محرّك 3D.
- `auto-quality`: إطارات بطيئة تخفض الجودة، وسريعة ترفعها.
- الرجوع إلى 2.5D والعودة إلى 3D دون أخطاء.

## المرحلة 12 — نظام الحفظ والتحميل (Save/Load System)
- **الخانات:** تلقائي (SAVEKEY، كما قبل — توافق خلفي) + 3 خانات يدوية (`_s1.._s3`) + حذف لكل خانة.
- **الحفظ التلقائي:** كل 25 ثانية + عند الانتقال بين المشاهد + عند الإقلاع الجديد + **عند إغلاق اللعبة** (beforeunload).
- **حفظ/تحميل سريع:** F5 (إلى خانة التلقائي) وF9 (الأحدث). لوحة خانات في قائمة الإيقاف والقائمة الرئيسية.
- **ما يُحفظ:** موضع اللاعب + حالته الكاملة (صحة/إشعاع/بطارية/تحمّل [كان يُهدر!]/كشاف/اتجاه/ميلان الجسم/انحناء/إصابة/رأسي) + المخزون + تقدم القصة + الألغاز + الأحداث + الأبواب والعناصر المستخدمة + **حالة الأعداء (موضع/موت/صحة عبر flags.pos — العدو الميت لا يُبعث)** + **الإعدادات** (تسافر مع الحفظ وتُستعاد عند التحميل) + الزمن والزيارات والإحصاءات.
- **`loadGame()` بلا معامل يحمّل الأحدث** (طوابع زمنية تصاعدية صارمة تمنع تعادل الميلي ثانية). حمّلة v1 القديمة تعمل (توافق خلفي).
- **زر «متابعة»** يتفعّل/يُعطّل بشكل idempotent عند كل إقلاع.
- اختبار «الإغلاق وإعادة الفتح»: عمليتا Node منفصلتان — الأولى تلعب وتحفظ وتُغلق (تفريغ المخزن على القرص)، والثانية سياق جديد كليًا يبذر localStorage قبل تحليل الكود (كالمتصفح تمامًا) ثم «متابعة» والتحقق من استعادة كل شيء (t_reopen_a/b).

## المرحلة 12b — معايرة رؤية الليل (إصلاح عاجل)
**الشكوى:** «كل شيء أسود». تحليل لقطة شاشة المستخدم بالبكسلات: المشهد يُرسم فعلًا (10,829 بكسل برتقالي = نيران التحطّم تعمل) لكن 91% من الشاشة بين سطوع 4-64/255 — أي «أسود» عمليًا.
**الإصلاحات:**
1. **قمر خافت** (`E3.moon` — DirectionalLight أزرق 0.24، بلا ظلال، خارجي فقط) يمنح المشهد عمقًا واتجاهًا.
2. **رفع أرضية الليل:** hemi 0.20→0.38، amb 0.05→0.10 خارجيًا (الداخل 0.11/0.045)، ضباب أقل كثافة (0.048→0.040) ولونه أفتح `0x161c26` (أزرق قمري) حتى تُقرأ الظلال ضدّه.
3. **الكشّاف كان يصوّب نحو الأفق!** الآن يصوّب **نحو الأرض** أمام اللاعب (هذا هو سبب «لا أرى شيئًا» رغم أن الكشاف مضاء) + أقوى (11→15) + أوسع (0.46→0.54) + ينطفئ أثناء الاختباء + وميض عند احتضار البطارية.
4. **نيران التحطّم أقوى** (a: .30→.46 / .22→.34 / .16→.22) + ضوء شعلة الشيطان (sprite) 5→7.
5. **تليين الحواف السوداء** (vignette 55%→42%).
**الاختبار الجديد:** `t_vis.js` (فحوص القمر/الأرضية/الضباب/تجويف الكشاف/النيران) يعمل على الـ stub **وعلى three.min.js الحقيقية r160** (اكتشفنا فرق ColorManagement: r/g/b خطي في الحقيقية — الفحوص عبر getHex()). الانحدار: 16 مجموعة + إعادة الفتح، كلها خضراء.
